#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

void ft_print_comb(void){ //declaration fonction comb
	int *c = NULL; //liste 
	int lc = 1;//longueur liste
	c = realloc(c,lc * sizeof(int));//realoc taille liste

		

	for (int i =0;i<1000;i++){//
		lc++;//
		c[i] = i;//
		c =realloc(c,lc* sizeof(int));//

		}
	
	int *a= NULL;//
	int la= 1;     //     

	a = realloc(a,la * sizeof(int));
	int Ao  = 5;
	
	 int first = 1;
	for (int i =0;i<1000;i++){
		if(c[i]== -1)
		continue;
		int backup = c[i];
		int Aa = c[i] / 100 ;
		int Ba = c[i] / 10;
		int Ca = c[i];

		Ba = Ba - Aa*10;
		Ca = Ca - Aa*100;
		Ca = Ca - Ba*10;
	
		if (!(Aa < Ba && Ba < Ca))
		continue;
		for(int y = 0;y < 1000;y++){
		 if (c[y] == -1){
                        continue;
                }

		int Xa = c[y]/100;
		int Xb = c[y]/10;
		int Xc = c[y];
		Xb = Xb - Xa*10;
		Xc = Xc - Xa*100;
		Xc = Xc - Xb*10;
		
		
		if(Xa == Aa || Xb == Aa || Xc == Aa  )
		
			if( Xa == Ba || Xb == Ba || Xc == Ba )
		 
				if( Xa == Ca || Xb == Ca || Xc == Ca )
		 
				                c[y]=-1;   
		
		}
	 if (!first)
            printf(", ");
        printf("%d%d%d", Aa, Ba, Ca);
        first = 0;

	

			}	


		

				}



int main(void){
	ft_print_comb();
	return 0;
}
